<h1>Welcome to the Ingredient Index</h1>

<h1>All Ingredients</h1>

<% @ingredients.each do |ingredient| %>
    <%= link_to ingredient.name, edit_ingredient_path(ingredient) %></br>
    </br>
<% end %>

<%= link_to 'Create an Ingredient', new_ingredient_path %></br>

<h1>Head back to one of your Recipes!</h1>

<% @recipes.each do |recipe| %>
    <%= link_to recipe.name, user_recipe_index_path(recipe) %>
<% end %>